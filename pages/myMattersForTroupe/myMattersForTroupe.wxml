<top-msg-tips id="topMsgTips"/>
<view class="container">
  <view class="swiper-tab">
      <view class="swiper-tab-item {{currentTab==0?'active':''}}" data-current="0" bindtap="clickTab">流程审批</view>
      <view class="swiper-tab-item {{currentTab==1?'active':''}}" data-current="1" bindtap="clickTab">预约场厅</view>
      <view class="swiper-tab-item {{currentTab==2?'active':''}}" data-current="2" bindtap="clickTab">剧目受约</view>
  </view>
  <include wx:if="{{currentTab==0}}" src="./_workflow.wxml"/>
  <include wx:if="{{currentTab==1}}" src="./_order.wxml"/>
  <include wx:if="{{currentTab==2}}" src="./_about.wxml"/>
  <!-- 底部导航 -->
  <df-bottom-nav navActive="3" isTroupe></df-bottom-nav>
</view>
